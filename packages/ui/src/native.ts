/**
 * `@nutrimero/ui/native` — bundled assets that only Metro can load (font files). Apps import it at
 * their composition root; everything else, tests included, imports the Node-loadable main entry.
 */
export { fontAssets } from "./font-assets";
